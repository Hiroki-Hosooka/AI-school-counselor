# CLAUDE.md

このファイルは Claude Code が自動で読み込みます。
**作業を始める前に必ず全文を読んでください。特に「変えてはいけない設計判断」の節は、
一見して改善に見える変更が実際には危険である理由を書いています。**

---

## 1. このプロジェクトは何か

中学生・高校生向けの、文字ベースのスクールカウンセリング支援AI。
高校の探究活動として開発し、年度内に校内での実証実験を目指しています。

### 目的（ここを外さない）

カウンセラーの代わりを作るのではありません。
**人に相談する一歩を踏み出せない生徒が、最初の一歩を踏み出すためのきっかけ**を作ります。

インタビューした心理士の言葉を借りると、ゴールは「完結」ではなく **to be continued**。
AIが問題を解決するのではなく、**人につなぐバトンを渡すこと**が役割です。

### したがって

- AIが最終的な受け皿になってはいけない
- AIだけが相談相手という状態（依存）を作ってはいけない
- 敷居を下げることが目的なので、ログイン・実名・登録を要求しない

---

## 2. 構成

**2026年9月に、単一HTML + Supabase Edge Function構成から Next.js(Vercel)構成に移行した。**
判断ロジック・プロンプト・安全層をサーバ側に置く原則、DBの構造(RLS含む)、ログインなしの設計は変えていない。
変わったのは実行場所だけ:Edge Function(Deno)→ Route Handler(Next.js/Node)、GitHub Pages → Vercel。
Supabase は Postgres(データ)としてのみ残っている。

```
ブラウザ(Next.jsのpage.tsx・表示のみ)
   │  fetch("/api/chat")  ※同一オリジンなのでCORSの仕組みは無い
   ▼
Next.js Route Handler  src/app/api/chat/route.ts(Vercelにデプロイ)
   │  ・GEMINI_API_KEY を保持
   │  ・DB からナレッジを読んでプロンプトを組む
   │  ・入力フィルタ → 生成 → 出力チェック
   │  ・会話 / 見立て / 安全判定を保存
   │  ・危機判定なら人へ通知
   ▼
Postgres(Supabase。データベースとしてのみ使う)
   knowledge / knowledge_history / person_memory / sessions / messages / safety_events
```

```
.
├── CLAUDE.md              このファイル
├── README.md              セットアップ手順
├── package.json / tsconfig.json / next.config.ts / eslint.config.mjs / .gitignore
├── src/
│   ├── safety.mjs         CRISIS_WORDS / OUTPUT_NG(route.tsとテストスクリプトの共通モジュール)
│   └── app/
│       ├── layout.tsx     フォント・メタデータ
│       ├── page.tsx       クライアント(表示のみ)
│       ├── globals.css
│       └── api/chat/route.ts   判断ロジック・プロンプト・安全層・DB読み書き
├── db/
│   ├── schema.sql                    テーブル定義・トリガ・ビュー
│   ├── seed_knowledge.sql            ナレッジ初期データ 140件
│   ├── seed_knowledge_structured.sql 構造化面接AI統合 手順3で追加した29件(出典 技/嶋)
│   ├── seed_knowledge_safety.sql     構造化面接AI統合 手順4で追加した4件(出典 設。D3〜D6)
│   └── knowledge.json                バックアップ(140件時点。上記の追加分は未反映)
├── scripts/
│   └── test-output-check.mjs
└── docs/
    ├── backlog.md         優先順位つきタスク
    └── interviews/        逐語(マスキング済み)
```

### 技術選択の理由

- **Next.js(Vercel)+ Supabase(DBのみ)** — フロントとバックエンドを1つのデプロイ先(Vercel)にまとめ、DBだけSupabaseに残す構成。旧構成にあった「デプロイ先を増やさない」という制約は、この移行によって「Vercel + Supabase の2つより増やさない」という形に変わったものとして運用する
- **ログインなし** — 敷居を下げることが目的。端末ごとの匿名UUIDを「引き継ぎコード」として使う。移行後も変えていない
- **RLS はポリシーを一つも作らない** — クライアントはDBに直接触らない。anon から完全に見えない状態が最も安全。Route Handler は service_role で迂回する(以前はEdge Functionが迂回していたのと同じ仕組み)
- **Geminiモデルは単一指定ではなくフォールバックリスト**(`route.ts` の `PRIMARY_MODELS`/`LITE_MODELS`)— 無料枠のレート制限とGoogle側のモデル退役(Gemini 2.0系は2026年6月に退役済み)の両方に対応するため。環境変数ではなくコードで管理しているので、モデルが使えなくなったら該当行を削除・追加する

---

## 3. ナレッジベース

`knowledge` テーブル。173件(構造化面接AI統合前は140件)。**コードに知識を書かないこと。**
追加・修正はすべてDBへ。

### 出典タグ（`src`）

| 値 | 内容 | 流派 |
|---|---|---|
| `嶋` | 嶋先生インタビュー（2026/5/28・2026/9） | 来談者中心寄り |
| `石` | 石田先生インタビュー | 人間性心理学ベース・折衷。小中とその保護者を担当 |
| `嶋石` | 二者が一致した項目。**最も強い根拠** | — |
| `理` | JILPT 資料シリーズ No.165 | 文献(複数流派) |
| `設` | 二者の証言をAI向けに読み替えた設計判断 | — |
| `技` | 技法・プロトコル理論資料(構造化面接AI統合。出典URLは`note`列) | CBT/SFBT等、`mode`列で紐づく |

**流派の中でも根拠の強さは異なります。** `技` は現時点でカウンセラーへの取材による検証を
経ていない一次資料(学術論文・公的機関の解説等)なので、`嶋石` はもちろん `嶋`/`石` 単独より
根拠として弱いものとして扱ってください(第8節「まだ埋まっていない穴」参照)。

**流派が違うと前提が違います。** 出典を混ぜてチャンク化すると矛盾した応答が出るので、
新しい知識を足すときは必ず `src` と `school` を埋めてください。

### 種別（`cat`）

`principle`（原則） / `ng`（禁止） / `stance`（関わりの型） / `ask`（問い） /
`resp`（応答技法） / `read`（見立て） / `role`（役割） / `verbatim`（**逐語**） /
`limit`（限界） / `ctx`（文脈）

`verbatim` は実際のカウンセラーの発話です。**要約せず、言い回しをそのまま保つこと。**
要約した瞬間に「原則の集合」に戻り、実装に使えなくなります。

### 禁止の強度（`lv`）

石田先生が「NGが難しい」「絶対バツではない」「ちょっと威圧感」とグラデーションで答えたため、
二値ではなく三段階にしています。

- `3` 絶対にしない
- `2` 避ける
- `1` 好ましくない（間違いではないが、できれば選ばない）

### 重心（`weight`）

`rapport` 関係をつくる / `main` 主訴を見極める / `goal` 目標を立てる / `plan` 作戦会議 / `any`

**これは順序ではありません。** 重なり合うもので、行き来します。

---

## 4. 対話設計（非構造化）

固定の段階を消化させる作りにはしていません。毎ターン、モデル自身が次を判断します。

### 関わりの型(`relation`)— 方針変更の駆動役

| 型 | 状態 | 対応 |
|---|---|---|
| `visitor` | 問題を表明しない／解決を期待していない | 解決へ急がない。来てくれたこと自体をねぎらう。**行動を求めない**。雑談に逃げてよい |
| `complainant` | 不満はあるが自分は変えられないと感じている | 不満に共感するが**同調はしない**。**本人に行動を求めない**。観察を促すにとどめる |
| `customer` | 自分の問題として動く用意がある | ここで**初めて**具体的な行動の話が生きる |

型は会話中に変わります。型が変わったのに対応が前のままだと、途端にずれます。

### 問いの層(`question_level`)

`none`(問わず受け止めのみ)→ `data`(事実)→ `diagnostic`(仮説の確認)→ `confrontational`(見立てで介入)

**層が上がるほど関係のできぐあいが要ります。** 迷ったら下の層に留めるか、問わないこと。

### 役割(`role`)

`listen` / `assess` / `inform`

`inform` は特に慎重に。「教えたい欲求は、一段上でいたい願望を満たす罠にもなる」(JILPT)。
答えを教えるより、答えを導く道を一緒に見つけるほうが支援的なことが多い。

---

## 5. 変えてはいけない設計判断

**以下はすべて、良かれと思って変更されやすい箇所です。変更前に必ず人間に確認してください。**

### 5.1 受け止めと同調を混ぜない

石田先生の指摘が設計の核心です。

> 何も考えずに同調してしまうのはリスク。ChatGPTなんかはそっちの方を求めていっちゃう。
> 一面でしか物事を見られなくなって、視野が狭くなってしまう。カウンセリングは逆で、
> できるだけ視野を広げてもらうのが目的。

生徒がChatGPTに行くのは否定されないからで、**その快適さがカウンセリングの目的と逆を向いています。**

- 「あの人ひどいね」「あなたは悪くないよ」は**同調であり禁止**
- 代わりに出来事を聞くか、本人の願いに角度を変える
  (「そう感じるエピソードがあったの？」「その時、どうしてほしかったの？」)

出力チェックの正規表現で機械的にも検知しています。**この正規表現を緩めないこと。**

### 5.2 危機時にリスクアセスメントを実装しない

石田先生は危機時、方法・時期・場所をどこまで具体的に考えているかを確認します。
**しかしAIには実装していません。** ナレッジID `D1` `D2` に理由を記録済み。

理由：この手順は、訓練を受けた人が、対面で、傷を見て、学校につなげられる状態でのみ機能します。
AIが同じ問いを投げると**方法を語らせる会話**になりかねず、しかもその先に接続先がありません。

取り入れたのは**態度だけ**です。

- 淡々と受ける(動揺を見せない)
- 話せたこと自体を評価する(「よくここで言えたね」)
- 人につなぐ

危機判定時は**生成を完全にスキップ**して固定応答を返します。この分岐を外さないこと。

### 5.3 通知に相談本文を含めない

`CRISIS_WEBHOOK_URL` に送るのはセッションIDと時刻だけです。
未成年の相談内容をSlackチャンネルに流すのは避けるべきなので、詳細はDBを見る運用にしています。

### 5.4 ログインを追加しない

敷居を下げることが目的です。ログインを付けると、嶋先生の言う
「敷居のないところで出会う」という構造そのものが壊れます。

### 5.5 「頑張って」を復活させない

> 万策を講じてきた人に「そうは言わんと頑張りな」というような励ましは使いたくない(嶋)

相談してくる人はすでにやれる限りを試した結果として来ています。

### 5.6 「いつでも」の使い分け

一見矛盾しますが、別物です。

- 依存文脈 → 「いつでも、いくらでも」は**絶対に言わない**(境界が壊れる)
- クロージング → 「また何かあったら、いつでもどうぞ」は**言う**(扉は開けておく)

正規表現は `いつでも(電話|連絡|来て|話しかけ|話して)` に絞ってあります。
広げると後者まで潰れるので注意。

### 5.7 APIキーをクライアントに置かない

`src/app/page.tsx`(クライアントコンポーネント)に API キーを書かない。環境変数に `NEXT_PUBLIC_` を付けない
(付けた瞬間ブラウザに埋め込まれる)。`GEMINI_API_KEY` と `SUPABASE_SERVICE_ROLE_KEY` は
`src/app/api/chat/route.ts`(サーバ側)からしか読まない。

旧構成にあった `ALLOWED_ORIGIN`(CORS)は、フロントとバックエンドが同一オリジンになったため廃止した。
別サイトからの無断利用という脅威モデル自体が成立しなくなったための削除であり、緩めたわけではない。

### 5.8 人単位の記憶は「上書きされる要約」であり、生ログの蓄積ではない

`person_memory` テーブルは、その人についての引き継ぎメモを1件だけ持つ。
セッションが閉じるたび(または `SESSION_GAP_MINUTES` 以上の空白後)に、
軽量モデルで要約を**再生成して上書き**する。追記ではない。

**これによりトークン量は構造的に増えない。** 「もっと詳しく覚えさせたい」という
理由で生の会話履歴を `person_memory` に足したり、上限文字数を外したりしないこと。

さらに、これは技術上の制約であると同時に**臨床的な判断**でもある。

> 人間は程よく大事なところだけ残って、そうじゃないところをうまく抜けさせていくのが
> 得意。その作業は難しいですよね(石田先生)

AIが前回の詳細を精度高く再生できてしまうと、5.4で述べた依存の懸念が強まる。
`buildSystem` には「自分からこのメモを詳しく話し出さない」という指示を必ず含めること。
記憶の精度を上げることが、常に良い改善とは限らない。

### 5.9 ログレビュー画面(admin.html)の認証を外さない

`public/admin.html` は心理士が会話ログそのものを閲覧・評価する画面。
`admin_sessions` / `admin_session_detail` は `ADMIN_TOKEN` による保護が必須。

**5.4「ログインを追加しない」は生徒側の話であり、この画面には適用しない。**
生徒の相談内容が見える画面を無保護にしないこと。逆に、生徒側の `src/app/page.tsx` に
この画面と同じ強度の認証を持ち込まないこと(敷居を上げてしまう)。

**`page.tsx` の設定パネル最下部に、admin.htmlへの小さなリンク(「スタッフ用ページ」)がある
(2026年9月・本人の依頼により追加)。** 合言葉(`ADMIN_TOKEN`)はリンクに埋め込んでいない
(開いた先の `?token=` で別途必要)。将来的には別URLに切り出す想定の暫定対応。この
リンクにより admin.html の存在は生徒からも(目立たない形だが)たどれるようになったため、
**`ADMIN_TOKEN` を "admin" のような推測されやすい値に絶対にしないこと。** リンクを
足す前から重要だったが、この変更で実質的な最後の防御線になっている。長く推測不能な値を
使い、URL(合言葉を含む)を共有しないこと(README参照)。

### 5.10 Gemini無料枠は合成テスト専用。生徒の会話には使わない

Gemini APIの無料枠は、送信データがGoogleの製品改善に利用される。
ペルソナAI同士の合成対話(機械だけのテスト)に使うのは問題ないが、
**実際の生徒が話す本番環境では絶対に使わない。** 要配慮個人情報を無断で
学習データに回すことになり、5.3「通知に相談本文を含めない」と同じ理由で禁止する。

本番は有料枠、大量の自動テスト(docs/backlog.md 1-3)は無料枠、と用途で明確に分ける。
コードやデプロイ設定上、この2つが混同されないようにすること。
具体的には環境変数を分ける(本番 `GEMINI_API_KEY` / テスト専用 `TEST_GEMINI_API_KEY`)。
可能であれば本番用キーと検証用キーを別のGoogle Cloudプロジェクトで発行すること。

### 5.11 Geminiの安全フィルターは、危機系カテゴリで必ず検証する

Gemini APIには有害カテゴリごとに閾値を設定できる安全フィルターがあり、
モデルの応答をこちらのロジックとは独立にブロックし得る。

このプロジェクトは自傷・希死念慮を扱うのが前提であり、デフォルト設定のままだと
危機的な発言に対して**返答自体がブロックされ、空の応答になる**リスクがある。
これは不十分な応答より悪い。相談してきた生徒が完全に無視された形になる。

このプロジェクトには既に自前の安全層(キーワード検知+分類器+固定応答)がある。
Google側の遮断は屋上屋であり、むしろ害になり得る。該当カテゴリの閾値は、
ラベル付きテストセットでのブロック率を実測したうえで、必要に応じて緩めること。
閾値を緩める判断は、必ずブロック率の実測値と併記して記録する(根拠のない緩和はしない)。

> 現状(2026年9月時点)、`callGeminiOnce` は HARASSMENT/HATE_SPEECH/SEXUALLY_EXPLICIT/
> DANGEROUS_CONTENT の4カテゴリを `BLOCK_ONLY_HIGH` に緩めている(Gemini移行直後、
> 支援的な文脈の相談内容が空応答になる不具合への対処として)。この変更はまだ
> ブロック率の実測(docs/backlog.md 1-3 テスト1)を経ていない。実測が出るまでの
> 暫定措置であることを踏まえ、実測後に閾値を見直すこと。

### 5.12 危機対応は一枚岩ではない(Tier A=selfだけが固定応答)

2026年9月(構造化面接AI統合 手順4)に、危機対応を `risk`(none/watch/crisis)に加えて
`subject`(self/other)でも分岐するようにした。**5.2の「生成を完全にスキップして固定応答」は、
`risk==="crisis" && subject==="self"`(本人の明確な危機サイン)のときだけ**を指す。
それ以外(下記の2つ)は生成を止めない。

- **Tier B(`risk==="watch"`)** — 「もう無理」「限界」「自分なんて」等、絶望感・無力感の表現
  (自殺念慮を明示しないもの)。以前は `CRISIS_WORDS` に「もう無理」「限界」を含め、自動的に
  Tier A(固定応答)扱いにしていたが、これは行き過ぎだった。「無理」は既存のMI系ナレッジ
  (M1/M3)が示す通り、両価性の表現として通常の会話にも頻出する。現在は生成を続け、
  `src/generate.mjs` の `buildSystem` がそのターンだけの指示ブロックを追加して、
  二択で程度を確認する質問(実質的なリスクアセスメント)をしないよう縛る(ナレッジ`D3`)。
- **第三者の安全懸念(`risk==="crisis" && subject==="other"`)** — 「友達が死にたいって」等、
  相談者自身ではなく友人・家族の安全が語られた場合。本人向けの窓口案内(`CRISIS_REPLY`)を
  そのまま使わず、ナレッジ`D5`/`D6`に沿って生成する。相談者自身にも同時にサインがある場合は
  `subject` が `self` に倒れ、通常のTier A(固定応答)が優先される
  (`src/classify.mjs` の `CLASSIFIER_PROMPT` 参照)。

**この2つに新しい固定応答を追加しないこと。** Tier Aの固定応答(`CRISIS_REPLY`)が特別なのは、
危機の場面でAIに言葉を選ばせないという安全側の設計だから(5.2)。Tier B/第三者はその逆で、
状況に応じた言葉が要るからこそ生成を残しており、代わりに「何をしないか」をナレッジと
プロンプトで縛っている。ここに固定応答を足すと、5.2の設計意図(なぜTier Aだけ特別か)がぼやける。

### 5.13 フェーズ1(インテーク)は台本、フェーズ2は自由。この境界を混ぜない

2026年9月(構造化面接AI統合 手順5)に、`sessions.phase`(intake/phase2)に応じて
`src/generate.mjs` の `buildSystem` が別々の「進め方」を出すようにした。

- **`phase==='intake'`のとき** — 主訴カテゴリ→背景・きっかけ→つらさスケール→期待する
  ゴールの4つを、1ターンにつき1つずつ集める、決まった台本(`buildIntakeBlock`)に従う。
  4節「対話設計(非構造化)」で述べている「毎ターンモデル自身が判断する」自由さは、
  意図的にここでは抑えている(構造化面接プロンプトの骨格をそのまま採用する、という
  Step1の確定判断による)。
- **`phase==='phase2'`のとき** — 従来通り、relation/weight/question_level/roleを
  モデルが毎ターン自分で判断する自由な進め方(`PHASE2_FLOW_BLOCK`)に戻る。

**この2つの境界を曖昧にしないこと。** intake中に自由な進め方を混ぜる、あるいは
phase2に台本を持ち込むと、どちらの設計判断も骨抜きになる。

このほか、実装上守っていること:

- **Turn1の選択肢に絵文字を使わない。** 参照した構造化プロンプト原文は`1️⃣2️⃣...`だが、
  本プロジェクトの既存原則(`buildSystem`「絵文字を使わない」)を優先し、半角数字にした。
- **モード名(CBT・SFBT等)は生徒に開示しない。** `recommended_mode`は内部の状態(DB)であり、
  生徒向けの応答に技法名を出す指示は一切ない(既存の`IS5`/`G6`と同じ原則の適用)。
- **フェーズ移行はモデルの自己申告だけで決めない。** `intake_complete`フラグを鵜呑みにせず、
  `applyIntakeUpdate`が主訴カテゴリ・背景・つらさスケール・ゴールの4項目とrecommended_modeが
  実際に(サーバ側のDB値として)揃っているかを確認してから`phase2`へ進める。ハルシネーションで
  早期に完了扱いにならないようにするため。
- **つらさスケールが4・5のときの安全確認は、コードの分岐ではなくプロンプトの指示に任せている。**
  「回答が4か5ならTurn4の前に安全確認する」という構造化プロンプトのルールは、
  `classify()`が生の数字(例:「5」)だけからは危機を判定できないため、`buildIntakeBlock`の
  文面でモデルに直接指示している。ここを「4か5という数字を検知したら自動でsafetyContext=
  tierBにする」ようなキーワード的な仕組みにはしていない(利用場面が偏るテキストヒューリスティック
  の追加は、5.11で戒めている「根拠のない変更」に近づくため。まずはこの形で運用し、
  実データで漏れが見えたら見直す)。

### 5.14 モード(技法)という軸は、ナレッジではなくプロンプトの骨格として持つ

2026年9月(構造化面接AI統合 手順6)に、`sessions.phase==='phase2'`のとき、
`src/generate.mjs` の `buildSystem` が `buildModeBlock(recommended_mode)` で
モード別(CBT/SFBT/NARRATIVE/ASSERTION/LISTEN_ONLY/PROBLEM_SOLVING/PSYCHOEDUCATION)の
「進め方の骨格」を追加するようにした。手順3時点(`db/seed_knowledge_structured.sql`の
コメント)で決めていた通り、`00_統合版_構造化面接AIプロンプト.txt` 17章の技法別ステージ・
質問例は、ナレッジ行としてではなく、`buildIntakeBlock`と同じやり方でプロンプト側に直接持つ。

- **既存の非構造化ナレッジ(嶋/石の逐語)を優先する(structured-unstructured-merge.md 全体方針①)。**
  これは「知識の優先順位」の問題であり、`retrieve()`のスコアリングだけで厳密に保証しよう
  としていない。出典`技`(技法カタログ)のタグは空、既存140件はmode列を持たない
  (`db/schema.sql` 9.1)という非対称な状態のため、mode一致のブーストを強くしすぎると
  `技`(取材未検証。3節参照)が`嶋石`の逐語より機械的に上位に来かねない。そこで
  `retrieve()`のmodeブーストは控えめ(+2。weight一致+1.5・verbatim+1.2と同程度)にとどめ、
  実際の優先順位判断は「参照できる知識」セクションの指示文(逐語を優先し、`技`は
  それで表現しきれない時の補足として使う)に委ねている。**この優先順位を、
  retrieve()のスコアだけで解決しようとして数値を強めないこと。**
- **モードは技法名としてユーザーに開示しない(00_統合版...txt 8章。既存のIS5/G6と同じ原則)。**
  `buildModeBlock`が組み立てる文面には、モデル向けの内部ラベルとしてモード名を含むが、
  「名称はユーザーに一切出さない」という指示を必ず添えている。
- **モードは複合可・毎ターン自動では再判定しない。** `recommended_mode`はTurn4直後の
  内部アセスメントで一度確定し(手順5)、以降は本人が自発的に進め方を変えたいと示した
  場合のみ、モデルの出力`mode_update`を通じて`applyModeUpdate`が更新する
  (17-5のLISTEN_ONLYからの移行を主な想定)。`intake_complete`と同様、モデルの自己申告を
  そのまま信じるのではなくサーバ側で`MODES`に含まれる値だけをフィルタしてから採用するが、
  `applyIntakeUpdate`のような複数項目の充足チェックは無い(本人の意思表示そのものが
  ゲートであり、複数スロットの整合性を待つ理由が無いため)。
- **フェーズ2の対話ルール(16章)のうち、この手順で入れたのは共通ルール(提案ピンポン禁止・
  例外の深掘り・学校生活の文脈・両価性へのMI的な聞き返し)とモード別の進め方まで。**
  クロージングの判断・要約の具体的な型(11章・18章のクロージング部分)は手順7で扱う
  範囲として、意図的にここでは実装していない(`sessions.closing_state`列は9.2で
  用意済みだが、手順6時点ではまだどこからも参照・更新していない)。
- **`loadKnowledge`の select 列に `mode` が無いと、このモードブーストは静かに機能しなくなる。**
  実装時に一度この見落としがあり、ローカルPostgresの実データで`retrieve()`を実行する
  検証で気づいた(APIキーが無い環境では、生成結果からは気づけない類の不具合だった)。
  ナレッジの列を増やすときは、`loadKnowledge`の select リストも必ず合わせて確認すること。

### 5.15 クロージング(`closing_state`)はハードなゲートではなく、モデルへのメモ

2026年9月(構造化面接AI統合 手順7)に、`00_統合版_構造化面接AIプロンプト.txt` 11章
(会話を終える際の基本ルール)を `src/generate.mjs` の `buildClosingBlock` として実装した。
AI側から一方的に「もう十分話せた」と判断して締めくくらない、技法上の手詰まりや
一つの提案への同意を会話終了の同意と混同しない、といった11章の禁止事項と、
「手詰まりを認める→Turn4のゴールに立ち返る→続けるか区切るかを本人に選んでもらう→
明確に区切りを望まれたときだけ要約に入る(結論は本人に語ってもらう)」という望ましい流れを
プロンプトにそのまま落とし込んでいる。

- **`sessions.closing_state`(9.2で用意済み)は、モデルが毎ターン申告する`closing_event`
  (none/asked/continue/close)を`applyClosingUpdate`がそのまま変換したものであり、
  `applyIntakeUpdate`のような複数スロットの充足確認はできない。** 「本人が本当に区切りを
  望んだかどうか」は自然文の意図判定そのものであり、intakeの4スロットのように客観的な
  DB値の充足確認で裏付けられる性質のものではないため。**したがって`closing_state`を、
  phase/intake_completeのような「これが揃うまで先に進めない」硬いゲートとして使わないこと。**
  位置づけはあくまで、次のターンのモデルに「前回どこまで話したか」を思い出させ、
  同じ確認をいたずらに繰り返させない・一度の「続ける」を蒸し返させない、という参考情報。
- **「区切り」という言葉が2つの意味で使われている。** 9節の用語集にある「区切り」
  (`sinceSummary`に基づく定期的な要約提案。5〜6ターンごと)と、11章由来の「今日の会話を
  終えるかどうか」は別物。`buildSystem`では「区切りの判断」(前者)と「クロージング」
  (後者)を別セクションのまま保っている。表現を統合しようとしないこと。
- **クロージング要約に添える窓口案内は、モデルに文言を作らせず、既存の固定データを
  再利用する。** `src/app/page.tsx` の `HOTLINES`(電話番号)はTier Aの`crisis-card`で
  既に使われている、確認済みの固定データ。11章が求める「しんどくなったらいつでもこの
  窓口に頼っていいよ」の一言を、モデルに毎回番号ごと書かせると、数字の書き間違い・表記ゆれの
  リスクがある(3節「ナレッジをコードにハードコードしない」の趣旨とも整合しない)。そこで
  `buildClosingBlock`ではモデルに具体的な窓口名・番号を書かせず、`closing_event==="close"`
  の応答には`messages.closing`を立てて、page.tsx側で`crisis-card`と同系統・別配色
  (`.closing-card`。警告色ではなくmoss系)の案内カードを重ねて表示する。
- **`messages.closing`は`crisis`と同じ扱いで`resume`/`admin.html`にも含めている**
  (ページ再読み込み後もカードが復元されるように)。

---

## 6. コーディング規約

- **日本語のコメントで書く**。チームメンバーとカウンセラーが読みます
- クライアントは**表示だけ**。判断ロジック・プロンプト・安全層をブラウザに戻さない
- ナレッジをコードにハードコードしない(DBが唯一の情報源)
- Next.js標準構成(next, react, react-dom, @supabase/supabase-js, eslint, typescript)以外の
  依存パッケージを不用意に増やさない。追加するときは何のためかをコミットメッセージに書く
- 秘密情報を絶対にコミットしない(`.env`、APIキー、逐語の未マスキング版)。`.gitignore` の `.env*` を外さない

### 変更時に必ず確認すること

安全層(`CRISIS_WORDS` / `OUTPUT_NG` / 危機分岐)に触れたら、必ずテストを流してください。

```bash
node scripts/test-output-check.mjs
```

「許容されるべき文」が誤検知されていないか、「検知されるべき文」が見逃されていないかを見ます。

---

## 7. RAGについて

**いまはベクトル検索を実装しないでください。**

140件規模ではタグ照合で十分です。1000件を超えるか、流派ごとの出し分けが必要になったときに検討します。
全文検索インデックス(`knowledge_body_idx`)だけは先に張ってあります。

そのときも:

- pgvector 単体ではなく**全文検索とのハイブリッド**にする。日本語の固有名詞や「作戦会議」のような特有の語はベクトルだけだと取りこぼす
- **`principle` と `ng` は検索対象にせず、常に全件をプロンプトに載せ続ける**。検索に回すと「その回だけ大事な原則が引かれなかった」という事故が起きる

---

## 8. まだ埋まっていない穴

知っておいてください。ここに関わる実装は慎重に。

1. **文字相談の経験者にまだ聞けていない** — 嶋先生はLINE相談を「自分の技量ではできない」と辞退、石田先生の電話相談は音声。**このプロダクトは文字相談**なのに、その領域の知見がない
2. **失敗経験が採れていない** — 「この返し方はまずかった」という証言がゼロ。禁止リストの根拠が弱い
3. **保護者面接** — ナレッジ0件。石田先生は担当しているので次回聞く
4. **評価AIの較正** — 心理士の採点と突き合わせていない。スコアを信用しないこと
5. **短文・沈黙への対応** — 対面なら表情で読めるが、文字では「乗ってこない」「言葉にできない」「帰りたい」の区別がつかない
6. **`技`(構造化面接AI統合で追加した技法カタログ)は取材による検証を経ていない** — CBT/SFBT等8技法・29件は学術論文・公的機関資料からの一次情報で、嶋先生・石田先生に「この言い回しで合っているか」を確認できていない。実際の会話で使う前に、機会を見て検証すること

---

## 9. 用語

| 語 | 意味 |
|---|---|
| 主訴 | 相談者が本当に相談したいこと。**表層の言葉は、その何段階か手前にすぎないことがある** |
| 見立て | アセスメント。仮説であり、確定させない |
| ラポール | 安心して話せる関係 |
| バウンダリー | 相談を受ける者と相談する人の境界。友達でも親でもない |
| 螺旋階段 | 同じ状況を繰り返しても対処が変われば高さが出ている、という嶋先生の比喩 |
| 作戦会議 | 本人が動く気になった後、「誰に」「いつ」「どう切り出すか」を具体化する段階(石田先生) |
| 区切り | 「今までの話、一回まとめてみようか」というターン。ないと話がズレていく |

---

## 10. 倫理・法務

- 相談内容は**要配慮個人情報**(未成年の心身の状態)
- 保存期間・アクセス範囲・削除手順を**DBに書き始める前に**確定させる
- いじめの疑いを認知した場合、学校には通報・組織的対応の義務がある(いじめ防止対策推進法)
- 危機判定が出たとき、誰がいつ見て何をするかを学校と取り決める。
  **AIが受け止めたのに誰にも届かない、が最悪の結果**

インタビューの逐語は、本人の同意を得て使用しています(AIの知識ベースとしての利用・探究成果としての発表・
個人情報がマスキングされた範囲での発言引用)。**マスキング前の音声・逐語をリポジトリに置かないこと。**
